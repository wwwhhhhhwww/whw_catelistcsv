# 门 A 复盘：我造了什么、学会了什么

> 日期：2026-09-21 ｜ 阶段：门 A（Python 地基 + 人肉基线）
> 一句话：这一阶段我用**纯 Python（无任何库）**，亲手搭了一条"催化剂成功率预测"的**人肉基线**，并学会了用 Git 管住它。

---

## 1. 我造了什么（每个一句话）

- **`ex_m1.py`**：自己造 9 行 CSV → 读回 → 按 `topology` 用 dict 分组 → 用函数统计每组的样本数 / 成功率 / 平均温度 → f-string 打表。
- **`mini_pipeline.py`**：**人肉基线预测器**——按 `topology` 学"多数类"规则，留最后 3 行当测试集算出**准确率 0.67**，再拿这条规则去给无标签的 `data_1.csv` 出建议（推荐器雏形）。
- **`robust_read.py`**：**抗脏数据读取器**——逐行 `try/except` + 合法性白名单检查，好的收下、坏的跳过、最后报告（10 行里 6 收 / 4 跳）。

---

## 2. 我学会的"新武器"

1. `with open(..., 'w'/'r') as f` —— 读写文件（`as f` 就是给文件起名字，等价于 `=`，好处是自动 close）
2. `readlines()` / `strip()` / `split(',')` —— 把文件变成可处理的数据
3. **列表推导式**（含带 `if` 过滤）—— `[x for x in 组 if x[3]=='1']`
4. **切片** —— `lines[1:]` 跳表头、`rows[-3:]` 取末尾 3 个
5. **dict 分组** —— key = 拓扑名，value = 该拓扑的所有行（一次解决"平行列表"的笨办法）
6. **函数** `def` + `return` 多个值（其实是元组）+ **元组解包** `n, ok, rate, avg = stats(rows)`
7. `for` + 累加 / `len()` / `int()` / `float()` 类型转换
8. **f-string 格式化** —— `{rate:.2f}`、`{avg:.1f}`
9. `while` 条件循环（+ 死循环意识：必须有让条件变假的语句）
10. `try` / `except` + `except (A, B)` 多类型捕获
11. **合法性白名单检查** —— `if success not in ('0','1')` 主动拦截沉默的脏数据
12. **读报错三步法** —— 找 `line N` → 看错误类型 → 看说明；五类常见错：NameError / SyntaxError / IndexError / TypeError / KeyError
13. **Git** —— `status` / `diff` / `add` / `commit` / `push` / `log` / `restore` / `revert` / `reset --hard`，以及 `.gitignore`（`.venv/` 永不入库）

---

## 3. 我踩过的坑

**坑 1：`as f` 到底啥意思**
- 现象：`with open(...) as f:` 里的 `as f` 一直看不懂，卡了好几天。
- 真相：`with open(...) as f` **完全等价于** `f = open(...)`——`as` 就是"起名字"。`with` 唯一多出来的好处是：缩进结束时**自动 `close()`**。
- 教训：面对"看不懂的语法"，先试着**拆成等价的最朴素写法**。

**坑 2：沉默的脏数据（最危险）**
- 现象：`dirty_data.csv` 里 `CHA,SDAOH,175,`（**末尾多一个逗号**），程序**没报错**就把它当合格数据收下了 → 结果变成 7 收 / 3 跳（预期 6 / 4）。
- 真相：末尾的逗号让它 `split(',')` 出 **4 格**，最后一格是**空字符串** `''`，`row[3]` 拿到了 `''` 而不报错。
- 修法：加**合法性白名单** `if success not in ('0','1')` —— 不是 `'0'`/`'1'` 一律当脏数据拦下。
- 教训：**不报错的脏数据，比报错的脏数据危险一百倍。** 程序只会跳过"它检查过的"东西。

**坑 3：PyCharm 自动导入的垃圾**
- 现象：文件头凭空多出 `from doctest import master`、`from os.path import split` 之类。
- 真相：敲名字时 PyCharm 弹"要不要导入"的建议，误接受就会塞进来；而 `'a,b'.split(',')` 是**内置方法，本来就不需要 import**。
- 教训：名字有红波浪线，**先怀疑拼写错误**，别急着导入。

---

## 4. 下一步（门 B）

- **装 pandas**：用现成的数据表工具，替掉我手写的 `readlines`/`split`/dict 分组。
- **装 TabPFN**：第一次用**真正的 AI 模型**做预测。
- **对比**：把 TabPFN 的准确率和我这条**人肉基线（0.67）**放一起比——看模型到底强多少、强在哪。
- **换真实数据**：从手写的 9 行玩具数据，换到 ZeoSyn（约 2.3 万条真实分子筛合成数据）。

---

## 附：当前仓库产出

| 文件 | 作用 |
|---|---|
| `ex_m1.py` | 读表 + dict 分组 + 统计 |
| `mini_pipeline.py` | 人肉基线 + 准确率 + 新实验建议 |
| `result.txt` | 基线结果（准确率 0.67） |
| `robust_read.py` | 抗脏数据读取 |
| `data.csv` / `data_1.csv` / `dirty_data.csv` | 数据（有答案 / 无答案 / 脏） |
| `.gitignore` | 忽略 `.venv/` 等 |
| `门A复盘.md` | 本文件 |
